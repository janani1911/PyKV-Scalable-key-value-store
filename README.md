# PyKV-Scalable-key-value-store
PyKV is a scalable in-memory key-value store built with Python and FastAPI, featuring LRU cache eviction and disk persistence. It supports CLI and Streamlit clients sharing a common backend, ensuring fast access, data consistency, and recovery after restarts.
